<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zitherharp.music.video">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_video_launcher"
        android:label="@string/video_app_name"
        android:roundIcon="@mipmap/ic_video_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Music">
        <activity
            android:name=".HashtagActivity"
            android:exported="false" />
        <activity
            android:name=".ArtistActivity"
            android:exported="false"
            android:label="@string/title_activity_artist"
            android:theme="@style/Theme.Music.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:theme="@style/Theme.Music.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".VideoActivity"
            android:exported="false"
            android:theme="@style/Theme.Music.NoActionBar" />
    </application>

</manifest>